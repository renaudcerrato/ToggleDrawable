<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/grey_light</item>
        <item name="searchArrowDrawableStyle">@style/CustomSearchArrowStyle</item>
    </style>

    <style name="CustomSearchArrowStyle" parent="SearchArrowDrawable">
        <!-- Customize your SearchArrowDrawable here-->
    </style>

</resources>
